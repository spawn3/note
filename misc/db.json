
db.getCollectionNames().forEach(function(name) {
    collection = db.getCollection(name);
    print(name + '        ' + collection.totalIndexSize());
    // print(collection.totalIndexSize());
    // print(collection.count());
    // printjson(collection.findOne());
});
